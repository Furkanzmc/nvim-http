if !exists("nvim_http_json_escape_utf")
    let g:nvim_http_json_escape_utf = v:true
endif

if !exists("nvim_http_result_vsplit")
    let g:nvim_http_result_vsplit = 1
endif

if !exists("nvim_http_focus_output_window")
    let g:nvim_http_focus_output_window = v:true
endif

if !exists("nvim_http_preserve_responses")
    let g:nvim_http_preserve_responses = v:false
endif
